{
  "name": "gitbook-plugin-link-checker",
  "version": "1.0.0",
  "description": "Plugin that checks for dead or absolute links",
  "main": "index.js",
  "engines": {
    "gitbook": ">3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jirkachadima-gitbook-plugin-link-checker.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "gitbook": {
    "properties": {
      "fqdn": {
        "type": "array",
        "default": "[]",
        "description": "List of domains that the checker should check. If any of those occur in a link, it's bad."
      },
      "dieOnError": {
        "type": "boolean",
        "default": "false",
        "description": "If set to true, the build will stop, otherwise the plugin will flush only warnings"
      }
    }
  }
}
